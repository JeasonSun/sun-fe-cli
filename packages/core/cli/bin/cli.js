#!/usr/bin/env node

const importLocal = require("import-local");
const log = require('@sun-fe/log');


log.error("error");
log.success("success");
log.notice("notice");

log.warn("warn");
log.info("info");

log.debug("debug");
log.verbose("verbose");